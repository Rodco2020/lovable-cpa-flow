
/**
 * Forecast Validation Utilities
 * Utilities for validating forecast data and system state
 */

/**
 * Mock implementation for validation
 */
export const estimateRecurringTaskInstances = (params: any) => {
  // Mock implementation for validation
  return [];
};

/**
 * Validate forecast system
 */
export const validateForecastSystem = async (): Promise<string[]> => {
  // Mock implementation for validation
  return [];
};

export default {
  estimateRecurringTaskInstances,
  validateForecastSystem
};
